Customize Odoo / odoo / `crm` module.

- Display for members of `sales_team.group_sale_salesman_all_leads` the
  following menu entries:
  - "CRM \> Analysis"
  - "CRM \> Configuration"
- Give access to `crm.stage`, `crm.tag` and `crm.lost.reason` models for
  members of `sales_team.group_sale_salesman_all_leads`.
