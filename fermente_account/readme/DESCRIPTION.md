Customize Odoo / `account` module.

- Hide the `analytic_distribution` field on all invoice views.
- On `account.move` form:
  - move fiscal position field from Other Info tab, to main form.
  - Hide useless fields. `invoice_incoterm_id`,
    `invoice_cash_rounding_id`.
